import { makeStyles } from "@mui/styles";

export const useStyleLoginModel = makeStyles((theme) => ({
  backDrop: {

  },
  modal: {
    width: "50%",
    height: "70%",
    background: "#fdfdfd",
    margin: "auto",
    padding: "0 2rem",
    borderRadius: "10px",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  },
}));
